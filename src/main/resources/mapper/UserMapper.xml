<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yzq.talespring.mapper.UserMapper">
    <insert id="insertUser" parameterType="int">
        insert into t_users(uid, username, password, email, home_url, screen_name, created, activated, logged,
                            group_name)
        values (#{uid}, #{username}, #{password}, #{email});
    </insert>
    <update id="createTable">
        create table if not exists `user`
        (
            `username`
            INTEGER,
            `password`
            varchar
        (
            20
        ) )
    </update>
    <select id="getUsers" resultType="User">
        select *
        from user
    </select>
    <select id="getUserById" resultType="User"></select>
</mapper>